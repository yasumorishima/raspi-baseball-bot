# OpenClaw config.yaml example
# APIキーは .env に記載し、このファイルには含めない
#
# ⚠️ 注意: heartbeat.schedulesセクションはOpenClawのcronに自動反映されません。
# スケジュールは `openclaw cron add` コマンドで登録してください。
# ここに書いてあるのはプロンプト内容の参考テンプレートです。

heartbeat:
  enabled: true
  max_daily_runs: 15
  quiet_hours:
    start: "23:00"
    end: "07:00"

# --- 以下は cronジョブ登録時のプロンプト参考例 ---
# openclaw cron add --cron "0 9 * * *" --tz "Asia/Tokyo" --name "WBC 09:00" \
#   --system-event "WBCの最新情報（試合結果・注目選手・大会の展開）を調べて140字以内でツイートして。"
#
# openclaw cron add --cron "0 8 * * *" --tz "Asia/Tokyo" --name "MLB結果" \
#   --system-event "昨日のMLB試合結果を調べて140字以内でツイートして。日本人選手の活躍を中心に。"
#
# openclaw cron add --cron "0 22 * * *" --tz "Asia/Tokyo" --name "NPB結果" \
#   --system-event "今日のNPB試合結果を調べて140字以内でツイートして。"
#
# openclaw cron add --cron "0 12,18 * * *" --tz "Asia/Tokyo" --name "移籍情報チェック" \
#   --system-event "NPB/MLBの最新ニュースや移籍情報を調べて140字以内でツイートして。特になければツイートしなくてOK。"
